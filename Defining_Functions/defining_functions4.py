"""
If you want your function to accept more than one parameter, you must separate each
parameter inside the parentheses with a comma.
"""


def f(x, y, z):
    return x * y + z

result = f(4, 5, 7)
print(result)