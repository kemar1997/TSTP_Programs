"""
The replace method replaces every occurrence of a string with another string. The first parameter is the string to
replace, and the second parameter is the string to replace the occurrence with:
"""

equ = "All animals are equal."
equ = equ.replace("l", "0")
print(equ)